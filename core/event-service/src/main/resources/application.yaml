spring:
  cloud:
    openfeign:
      circuitbreaker:
        enabled: true
  application:
    name: event-service
  config:
    import:
      - "optional:configserver:http://localhost:8888"
      - "optional:configserver:http://config-server:8888"